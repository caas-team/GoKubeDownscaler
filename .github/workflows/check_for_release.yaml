name: Check for new release

on:
  pull_request_target:
    types: [opened, synchronize, labeled, unlabeled]
    branches:
      - main

jobs:
  check_for_release:
    name: Check For Release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Check for appVersion changes
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Checking for appVersion changes..."
          base_app_version=$(gh api -H "Accept: application/vnd.github.raw" "/repos/${GITHUB_REPOSITORY}/contents/deployments/chart/Chart.yaml?ref=${PR_BASE_SHA}" | yq e '.appVersion' - | tr -d '\n\r')
          head_app_version=$(gh api -H "Accept: application/vnd.github.raw" "/repos/${GITHUB_REPOSITORY}/contents/deployments/chart/Chart.yaml?ref=${PR_HEAD_SHA}" | yq e '.appVersion' - | tr -d '\n\r')

          if [ "$base_app_version" != "$head_app_version" ]; then
            app_version_change="version changed"
          else
            app_version_change="No appVersion changes detected."
          fi

          echo "app_version_change=$app_version_change" >> "$GITHUB_ENV"

      - name: Validate semantic versioning
        if: ${{ env.app_version_change == 'version changed' }}
        env:
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          app_version=$(gh api -H "Accept: application/vnd.github.raw" "/repos/${GITHUB_REPOSITORY}/contents/deployments/chart/Chart.yaml?ref=${PR_HEAD_SHA}" | yq e '.appVersion' -)
          if ! echo "$app_version" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "::error::appVersion '$app_version' is not valid semantic versioning. Expected MAJOR.MINOR.PATCH (e.g. 1.3.0)"
            exit 1
          fi
          echo "appVersion '$app_version' correctly follows semantic versioning"

      - name: Remove new version label
        continue-on-error: true
        if: ${{ env.app_version_change == 'No appVersion changes detected.' }}
        run: |
          echo "No appVersion changes detected. Removing new version label"
          gh pr edit ${{ github.event.pull_request.number }} --remove-label "new release"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Fail if changes occurred
        if: ${{ env.app_version_change == 'version changed' && !contains(github.event.pull_request.labels.*.name, 'new release') }}
        run: |
          gh pr edit "${{ github.event.pull_request.number }}" --add-label "needs approval"
          echo "Version changed, exiting..."
          exit 1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Remove approval label
        run: |
          gh pr edit "${{ github.event.pull_request.number }}" --remove-label "needs approval"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  post_message:
    name: Post Message To Warn Of New Release
    runs-on: ubuntu-latest
    needs: check_for_release
    if: ${{ needs.check_for_release.result == 'failure' && !contains(github.event.pull_request.labels.*.name, 'needs approval') && github.event.action != 'labeled' && github.event.action != 'unlabeled' }}
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Extract appVersion
        id: extract_appversion
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          base_app_version=$(gh api -H "Accept: application/vnd.github.raw" "/repos/${GITHUB_REPOSITORY}/contents/deployments/chart/Chart.yaml?ref=${PR_BASE_SHA}" | yq e '.appVersion' - | tr -d '\n\r')
          head_app_version=$(gh api -H "Accept: application/vnd.github.raw" "/repos/${GITHUB_REPOSITORY}/contents/deployments/chart/Chart.yaml?ref=${PR_HEAD_SHA}" | yq e '.appVersion' - | tr -d '\n\r')

          if [ "$base_app_version" != "$head_app_version" ]; then
            appversion="$head_app_version"
          else
            appversion="$base_app_version"
          fi

          echo "appversion=$appversion" >> $GITHUB_ENV

      - name: Post warning comment
        uses: peter-evans/create-or-update-comment@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: "⚠️ Warning: Merging this PR will result in a new release because the `appVersion` in Chart.yaml has changed to `${{ env.appversion }}`. Please confirm this by adding the `new release` label before merging."
