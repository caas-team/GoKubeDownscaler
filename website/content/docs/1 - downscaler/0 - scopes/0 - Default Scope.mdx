---
title: Default Scope
id: default-scope
globalReference: docs-default-scope
---

# Default Scope

The Default Scope is the most generic scope available.
It is used when no other scope is specified and holds the default
values for all the configuration options.
The Default Scope only holds values

INSERT PYRAMID IMAGE

It is important to understand its default values since they will be used if no
other scope is specified.

## Values

The Default Values can be found in the values page.
