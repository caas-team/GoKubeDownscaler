{/* this doc acts as a redirect from /guides to the first actual doc in the sidebar */}

import { RedirectToFirstDoc } from "/src/components/FirstDocRedirect.tsx";

<RedirectToFirstDoc sidebar="GuidesSidebar" /> {/* <!-- markdownlint-disable-line no-inline-html --> */}
