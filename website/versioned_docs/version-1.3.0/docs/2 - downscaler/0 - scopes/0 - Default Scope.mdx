---
title: Default Scope
id: default-scope
globalReference: docs-default-scope
description: Learn how the default scope is defined in GoKubeDownscaler and how it can be overridden.
keywords: [default scope, default values]
---

import HierarchyDiagram from "./templates/_hierarchy-diagram.mdx";

# Default Scope

The **Default Scope** is the most generic scope available.
It contains the default values for all the configuration options which are used when no other scope specifies them.
It can be overridden by the [Env Scope](ref:docs-env-scope), [CLI Scope](ref:docs-cli-scope),
[Namespace Scope](ref:docs-namespace-scope) or [Workload Scope](ref:docs-workload-scope)

<HierarchyDiagram highlight="a" />

It is important to understand its default values
since they will be used if no other scope specifies them.

## Values

The Default Values can be found on the [values page](ref:docs-values).
